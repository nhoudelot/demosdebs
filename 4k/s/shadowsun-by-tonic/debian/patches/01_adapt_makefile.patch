--- /dev/null
+++ b/Makefile
@@ -0,0 +1,32 @@
+#variables de compilation
+SHELL := /bin/sh
+CC    := gcc
+CFLAGS   += -std=c99 -Wall -pipe $(shell pkgconf --cflags sdl glu)
+LDFLAGS  += $(shell pkgconf --libs sdl glu) -lm
+TARGET   := shadowsun-by-tonic
+#variable de nettoyage
+RM_F := rm -f
+#variables d'instalation
+INSTALL = install
+INSTALL_DIR     = $(INSTALL) -p -d -o root -g root  -m  755
+INSTALL_PROGRAM = $(INSTALL) -p    -o root -g root  -m  755
+prefix          = /usr
+exec_prefix     = $(prefix)
+bindir          = $(prefix)/bin
+
+SOURCE          = shadowsun.c
+
+export
+
+all: $(TARGET)
+
+$(TARGET):
+	 $(CC) $(SOURCE) $(CFLAGS) -o $(TARGET) $(LDFLAGS)
+
+install: $(TARGET)
+	$(INSTALL_DIR) $(DESTDIR)$(bindir)
+	-@$(RM_F) $(DESTDIR)$(bindir)/$(TARGET)
+	$(INSTALL_PROGRAM) $(TARGET) $(DESTDIR)$(bindir)
+
+clean:
+	-$(RM_F) $(TARGET)
--- /dev/null
+++ b/g.h
@@ -0,0 +1,5 @@
+#include <SDL/SDL.h>
+#include <SDL/SDL_opengl.h>
+#include <math.h>
+#include <stdlib.h>
+
