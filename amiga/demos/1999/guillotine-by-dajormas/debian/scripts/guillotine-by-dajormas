#!/bin/bash -e

source /usr/share/common-install-function-ddebs/common-install-function

domain=guillotine-by-dajormas

APP_FULLNAME="guillotine"
APP_COPYRIGHT="da Jormas"
APP_DESC="Demo ranked 7th at Mekka and Symposium 1999"

function run()
{
	/usr/bin/fs-uae /usr/share/$domain/$domain.fs-uae $1
}

if [ "$#" -gt "1" ]; then
	 NSH_std_usage_AMIGA
	exit 1
elif [ "$#" -lt "1" ]; then
	run
else
	case "$1" in
	--help)
		NSH_std_usage_AMIGA
		;;
	--fullscreen)
		run --fullscreen
		;;
	--version)
		NSH_version_AMIGA
		;;
	*)
		NSH_std_usage_AMIGA
		exit 1
		;;
	esac
fi

exit 0
