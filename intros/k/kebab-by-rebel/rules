#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/kebab-by-rebel.6.md -o debian/kebab-by-rebel.6
