#!/usr/bin/make -f

#export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@  --with python2

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/textperiment-by-pktteamgenshiken.6.md -o debian/textperiment-by-pktteamgenshiken.6
