Description: fix includes path & main()
Author: Nicolas HOUDELOT <nicolas@demosdebs.org>
Applied-Upstream: no
Last-Update: 2020-04-24
---
This patch header follows DEP-3: http://dep.debian.net/deps/dep3/
--- a/src/main.c
+++ b/src/main.c
@@ -1289,7 +1289,7 @@
 		//float fms = ticks * 0.000001f;
 		//glUniform3f(uniform_noise, fms, dsinf(1.4f* fms), 0.4f);
 
-		dsrand((unsigned int)beat);
+		dsrand((unsigned int)(uintptr_t)beat);
 
 		pos[0] = beat_strength;
 		pos[1] = dsinf(beat_strength) * 12.0f;
@@ -1338,7 +1338,7 @@
 void *__progname;
 /** \brief Object file starting point.
  */
-void _start()
+void main()
 {
 #endif
 #if defined(USE_LD)
@@ -1510,7 +1510,8 @@
 	{
 		//SDL_GL_SetAttribute(SDL_GL_DOUBLEBUFFER, 0);
 		int frame_idx = 0;
-		char filename[] = "dirty_garden_0000.png";
+		char filename[32];
+		snprintf(filename, sizeof(filename), "dirty_garden_%04u.png", (unsigned)frame_idx);
 		FILE *fptr = fopen("dirty_garden_audio.raw", "w");
 		fwrite(audio_buffer,
 				sizeof(int16_t),
--- a/src/record.c
+++ b/src/record.c
@@ -3,12 +3,12 @@
 #include <stdlib.h>
 #include <string.h>
 
-#include "png.h"
-#include "zlib.h"
+#include <png.h>
+#include <zlib.h>
 #ifdef __APPLE__
-#include "SDL/SDL_opengl.h"
+#include <SDL/SDL_opengl.h>
 #else
-#include "SDL_opengl.h"
+#include <SDL/SDL_opengl.h>
 #endif
 
 #include "record.h"
