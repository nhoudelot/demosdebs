--- a/main.c
+++ b/main.c
@@ -36,7 +36,8 @@
 		if(!strcmp(argv[1],"-w"))
 			flags-=SDL_WINDOW_FULLSCREEN;
 
-	if(argv[0][strlen(argv[0])-1]=='w')
+	if(argv[0] && strlen(argv[0]) > 0 &&
+	   argv[0][strlen(argv[0])-1]=='w')
 		flags-=SDL_WINDOW_FULLSCREEN;
 
 	SDL_Init(SDL_INIT_VIDEO|SDL_INIT_AUDIO|SDL_INIT_TIMER);
--- a/osa.c
+++ b/osa.c
@@ -126,7 +126,7 @@
 	glCopyTexSubImage2D(GL_TEXTURE_2D,0,
 	                    0,0, // destination
 	                    0,0, // source
-	                    W,H
+	                    (GLsizei)W,(GLsizei)H
 	                    );
 }
 
@@ -142,7 +142,7 @@
 	glCopyTexSubImage2D(GL_TEXTURE_2D,0,
 	                    0,0, // destination
 	                    0,0, // source
-	                    W,H
+	                    (GLsizei)W,(GLsizei)H
 	                    );
 	//glLoadIdentity(); Already set by motion blur
 	glBlendFunc(GL_ONE,GL_ONE);
@@ -208,7 +208,7 @@
 
 	/* Find the beat from demo script and load values */
 	beat/=8;
-	for (i=0; i<sizeof(osat)/N; i++)
+	for (i=0; i<(int)(sizeof(osat)/N); i++)
 		if (beat>=((unsigned char)osat[i*N+5]))
 			pat=i;
 
