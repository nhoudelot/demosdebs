Description: fix 64 bit portability
Generated-by: Claude Sonnet 4.6
Reviewed-by: Nicolas HOUDELOT <nicolas@demosdebs.org>
Applied-Upstream: no
Last-Update: 2026-04-18
--- a/src/cg_x11.h
+++ b/src/cg_x11.h
@@ -20,15 +20,15 @@
 /* Global variables for better coding style.
    We use cg_ prefix to identify these.  */
 
-Display                *cg_display;
-Window                  cg_window;
-XVisualInfo            *cg_visual;
-XRRScreenConfiguration *cg_screen_conf;
-int                     cg_orig_size;
-Rotation                cg_rotation;
-short                   cg_orig_rate;
-XEvent                  cg_event;
-KeySym                  cg_ks;
+extern Display                *cg_display;
+extern Window                  cg_window;
+extern XVisualInfo            *cg_visual;
+extern XRRScreenConfiguration *cg_screen_conf;
+extern int                     cg_orig_size;
+extern Rotation                cg_rotation;
+extern short                   cg_orig_rate;
+extern XEvent                  cg_event;
+extern KeySym                  cg_ks;
 extern char             cg_isrunning;
 
 void CreateScene(int width, int height);
--- a/src/cg_x11.c
+++ b/src/cg_x11.c
@@ -1,6 +1,17 @@
 #include <GL/gl.h>
 #include "cg_x11.h"
 
+/* One definition per variable declared extern in cg_x11.h */
+Display                *cg_display;
+Window                  cg_window;
+XVisualInfo            *cg_visual;
+XRRScreenConfiguration *cg_screen_conf;
+int                     cg_orig_size;
+Rotation                cg_rotation;
+short                   cg_orig_rate;
+XEvent                  cg_event;
+KeySym                  cg_ks;
+
 char cg_isrunning = 1;
 
 void SetScreenSize(int width, int height){
--- a/src/cg_music.h
+++ b/src/cg_music.h
@@ -3,13 +3,13 @@
 #include <alsa/asoundlib.h>
 
 /* Global audio handle */
-snd_pcm_t                *cg_audio;
+extern snd_pcm_t                *cg_audio;
 extern snd_pcm_uframes_t cg_frames;
 /* Music file */
-OggVorbis_File cg_music;
-char                     *cg_audio_buffer;
-int cg_audio_buffer_size;
-FILE                     *cg_music_file;
+extern OggVorbis_File cg_music;
+extern char                     *cg_audio_buffer;
+extern int cg_audio_buffer_size;
+extern FILE                     *cg_music_file;
 
 void InitMusic(char *musicfilename);
 void PlayingMusic(snd_pcm_t *handle);
--- a/src/cg_music.c
+++ b/src/cg_music.c
@@ -6,6 +6,13 @@
 #include "cg_music.h"
 #include "cg_x11.h"
 
+/* One definition per variable declared extern in cg_music.h */
+snd_pcm_t      *cg_audio;
+OggVorbis_File  cg_music;
+char           *cg_audio_buffer;
+int             cg_audio_buffer_size;
+FILE           *cg_music_file;
+
 snd_pcm_uframes_t cg_frames = 128;
 
 void InitMusic(char *musicfilename){
--- a/src/particle.h
+++ b/src/particle.h
@@ -15,7 +15,7 @@
 	float        *lifes;
 }Particle;
 
-GLuint particletex;
+extern GLuint particletex;
 
 /* Create one particle. The particle index is given in the num argument */
 void createParticle(float starttime, Particle *p, unsigned int num, float sx, float sy, float sz, float dx, float dy, float dz);
--- a/src/particle.c
+++ b/src/particle.c
@@ -4,6 +4,10 @@
 #include <stdlib.h>
 #include "texture.h"
 #include "particle.h"
+
+/* One definition for the texture handle declared extern in particle.h */
+GLuint particletex;
+
 /* Create one particle. The particle index is given in the num argument */
 void createParticle(float starttime, Particle *p, unsigned int num, float sx, float sy, float sz, float dx, float dy, float dz){
 
--- a/src/shader.c
+++ b/src/shader.c
@@ -36,21 +36,21 @@
 	FILE *sourcefile;
 	char *content = NULL;
 	GLuint ret;
-
-	int count=0;
+	long   filesize = 0;
+	size_t readcount = 0;
 
 	sourcefile = fopen(fn,"rt");
 
 	if (sourcefile != NULL) {
 
 		fseek(sourcefile, 0, SEEK_END);
-		count = ftell(sourcefile);
+		filesize = ftell(sourcefile);
 		rewind(sourcefile);
 
-		if (count > 0) {
-			content = (char *)malloc(sizeof(char) * (count+1));
-			count = fread(content, sizeof(char), count, sourcefile);
-			content[count] = '\0';
+		if (filesize > 0) {
+			content = (char *)malloc(sizeof(char) * ((size_t)filesize + 1));
+			readcount = fread(content, sizeof(char), (size_t)filesize, sourcefile);
+			content[readcount] = '\0';
 		}
 		fclose(sourcefile);
 	}
--- a/src/poligonexplode.h
+++ b/src/poligonexplode.h
@@ -6,7 +6,7 @@
 
 	float dpos[9]; /* Relative coordinates from the center of the triangle */
 	float nor[9]; /* Normal vectors */
-	float tex[9]; /* Texture coordinates */
+	float tex[6]; /* Texture coordinates (2 per vertex × 3 vertices) */
 } Triangle;
 
 Triangle *generateFromMesh(Mesh3D *mesh);
--- a/src/poligonexplode.c
+++ b/src/poligonexplode.c
@@ -20,9 +20,11 @@
 		for(j = 0; j < 3; j++) {
 			for(k = 0; k < 3; k++) {
 				v[j * 3 + k] = mesh->vertices[mesh->indices[i + j] * 3 + k];
-				triangles[i / 3].tex[j * 2 + k] = mesh->texcoords[mesh->indices[i + j] * 2 + k];
 				triangles[i / 3].nor[j * 3 + k] = mesh->normals[mesh->indices[i + j] * 3 + k];
 			}
+			for(k = 0; k < 2; k++) {
+				triangles[i / 3].tex[j * 2 + k] = mesh->texcoords[mesh->indices[i + j] * 2 + k];
+			}
 		}
 
 		triangles[i/3].cx = (v[0] + v[3] + v[6]) / 3.0;
--- a/src/aeronautism.c
+++ b/src/aeronautism.c
@@ -84,7 +84,7 @@
 	logo->normals  = malloc(sizeof(GLfloat) * WIDTH * HEIGHT * 3);
 	logo->colors   = malloc(sizeof(GLfloat) * WIDTH * HEIGHT * 4);
 	logo->texcoords= malloc(sizeof(GLfloat) * WIDTH * HEIGHT * 2);
-	logo->indices  = malloc(sizeof(GLfloat) * WIDTH * HEIGHT * 6);
+	logo->indices  = malloc(sizeof(GLuint)  * WIDTH * HEIGHT * 6);
 
 	logo->vcount   = WIDTH * HEIGHT * 3;
 	logo->ncount   = WIDTH * HEIGHT * 3;
