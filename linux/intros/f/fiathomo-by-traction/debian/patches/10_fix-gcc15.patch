Description: fixing various warnings 
Generated-by: Claude Sonnet 4.6
Reviewed-by: Nicolas HOUDELOT <nicolas@demosdebs.org>
Applied-Upstream: no
Last-Update: 2026-04-14
---
This patch header follows DEP-3: http://dep.debian.net/deps/dep3/
--- a/src/beziercurve.cpp
+++ b/src/beziercurve.cpp
@@ -2,9 +2,9 @@
 
 QuadraticBezierCurve::QuadraticBezierCurve()
 {
-	P1 = Vector::Vector(0,0,0);
-	P2 = Vector::Vector(0,0,0);
-	P3 = Vector::Vector(0,0,0);
+	P1 = Vector(0,0,0);
+	P2 = Vector(0,0,0);
+	P3 = Vector(0,0,0);
 }
 
 QuadraticBezierCurve::QuadraticBezierCurve(Vector &point1, Vector &point2, Vector &point3)
@@ -54,10 +54,10 @@
 
 CubicBezierCurve::CubicBezierCurve()
 {
-	P1 = Vector::Vector(0,0,0);
-	P2 = Vector::Vector(0,0,0);
-	P3 = Vector::Vector(0,0,0);
-	P4 = Vector::Vector(0,0,0);
+	P1 = Vector(0,0,0);
+	P2 = Vector(0,0,0);
+	P3 = Vector(0,0,0);
+	P4 = Vector(0,0,0);
 }
 
 CubicBezierCurve::CubicBezierCurve(Vector &point1, Vector &point2, Vector &point3, Vector &point4)
--- a/src/font.cpp
+++ b/src/font.cpp
@@ -68,6 +68,7 @@
 	else if (!strcmp("Anna",string)) { return 21; }
 	else if (!strcmp("matt current",string)) { return 22; }
 	else if (!strcmp("gluterol",string)) { return 23; }
+	return -1;
 }
 
 void font_write(float xpos, float ypos, const char *string)
--- a/src/Effects/amino.cpp
+++ b/src/Effects/amino.cpp
@@ -49,7 +49,8 @@
 		glBegin(GL_LINE_STRIP);
 		for (j=0;j<asti;j++)
 		{
-			glVertex3fv((float *)&(aminodata1[i][j]*kokomul));
+			Vector vtmp1 = aminodata1[i][j]*kokomul;
+			glVertex3fv((float *)&vtmp1);
 		}	
 		glEnd();
 		glColor4f(0.7f, 0.3f, 0.3f, alpha*alpha*0.4f);
@@ -57,7 +58,8 @@
 		glBegin(GL_POINTS);
 		for (j=0;j<asti;j++)
 		{
-			glVertex3fv((float *)&(aminodata1[i][j]*kokomul));
+			Vector vtmp2 = aminodata1[i][j]*kokomul;
+			glVertex3fv((float *)&vtmp2);
 		}
 		glEnd();
 
--- a/src/Effects/kukka.cpp
+++ b/src/Effects/kukka.cpp
@@ -15,8 +15,6 @@
 	glLoadIdentity();
 	glDisable(GL_DEPTH_TEST);
 
-	const float rotangle = aika*0.4f;
-
 	const float rotradius = 1.8f + 0.6f*(float)sin(aika*16);
 
 	Vector campos = Vector(rotradius*(float)cos(aika*8), 
@@ -110,15 +108,19 @@
 		glBegin(GL_TRIANGLE_STRIP);
 		for (j=0;j<petalvertices;j++)
 		{
-			glVertex3fv((float *)&(pos+centerpoints[j]));
-			glVertex3fv((float *)&(pos+points1[j]));
+			Vector vtmp_c1 = pos+centerpoints[j];
+			Vector vtmp_p1 = pos+points1[j];
+			glVertex3fv((float *)&vtmp_c1);
+			glVertex3fv((float *)&vtmp_p1);
 		}
 		glEnd();
 		glBegin(GL_TRIANGLE_STRIP);
 		for (j=0;j<petalvertices;j++)
 		{
-			glVertex3fv((float *)&(pos+centerpoints[j]));
-			glVertex3fv((float *)&(pos+points2[j]));
+			Vector vtmp_c2 = pos+centerpoints[j];
+			Vector vtmp_p2 = pos+points2[j];
+			glVertex3fv((float *)&vtmp_c2);
+			glVertex3fv((float *)&vtmp_p2);
 		}
 		glEnd();
 
@@ -130,13 +132,15 @@
 
 		for (j=0;j<petalvertices;j++)
 		{
-			glVertex3fv((float *)&(pos+points1[j]));
+			Vector vtmp_lp1 = pos+points1[j];
+			glVertex3fv((float *)&vtmp_lp1);
 		}
 		glEnd();
 		glBegin(GL_LINE_STRIP);
 		for (j=0;j<petalvertices;j++)
 		{
-			glVertex3fv((float *)&(pos+points2[j]));
+			Vector vtmp_lp2 = pos+points2[j];
+			glVertex3fv((float *)&vtmp_lp2);
 		}
 		glEnd();
 	}
@@ -147,8 +151,10 @@
 	glBegin(GL_LINES);
 	for (i=0;i<hedecount;i++)
 	{
-		glVertex3fv((float *)&(heteet[(i<<1)]*kohta+pos));
-		glVertex3fv((float *)&(heteet[(i<<1)+1]*kohta+pos));
+		Vector vtmp_h0 = heteet[(i<<1)]*kohta+pos;
+		Vector vtmp_h1 = heteet[(i<<1)+1]*kohta+pos;
+		glVertex3fv((float *)&vtmp_h0);
+		glVertex3fv((float *)&vtmp_h1);
 	}
 	glEnd();
 }
--- a/src/Effects/ihminen.cpp
+++ b/src/Effects/ihminen.cpp
@@ -67,25 +67,30 @@
 		else
 			glBlendFunc(GL_SRC_ALPHA, GL_ONE_MINUS_SRC_ALPHA);
 
+		Vector wv1 = pos+v1;
+		Vector wv2 = pos+v2;
+		Vector wv3 = pos+v3;
+		Vector wv4 = pos+v4;
+
 		glBegin(GL_QUADS);
 		glTexCoord2f(0,1);
-		glVertex3fv((float *)&(pos+v1));
+		glVertex3fv((float *)&wv1);
 		glTexCoord2f(0,0);
-		glVertex3fv((float *)&(pos+v2));
+		glVertex3fv((float *)&wv2);
 		glTexCoord2f(1,0);
-		glVertex3fv((float *)&(pos+v3));
+		glVertex3fv((float *)&wv3);
 		glTexCoord2f(1,1);
-		glVertex3fv((float *)&(pos+v4));
+		glVertex3fv((float *)&wv4);
 		glEnd();
 
 		glDisable(GL_TEXTURE_2D);
 		glColor4f(1,1,1,alpha*0.4f);
 		glBlendFunc(GL_SRC_ALPHA, GL_ONE);
 		glBegin(GL_LINE_LOOP);
-		glVertex3fv((float *)&(pos+v1));
-		glVertex3fv((float *)&(pos+v2));
-		glVertex3fv((float *)&(pos+v3));
-		glVertex3fv((float *)&(pos+v4));
+		glVertex3fv((float *)&wv1);
+		glVertex3fv((float *)&wv2);
+		glVertex3fv((float *)&wv3);
+		glVertex3fv((float *)&wv4);
 		glEnd();
 	}
 
--- a/src/Effects/atomi.cpp
+++ b/src/Effects/atomi.cpp
@@ -159,7 +159,8 @@
 			const float tval = kohta * 2*3.141592f;
 
 			Vector p = Vector((float)sin(kohta), (float)cos(kohta), 0) * radii[i];
-			glVertex3fv((float *)&(position + (p*rotate)*globalrotate));
+			Vector vtmp = position + (p*rotate)*globalrotate;
+			glVertex3fv((float *)&vtmp);
 		}
 		glEnd();
 	}
--- a/src/Effects/molekyyli.cpp
+++ b/src/Effects/molekyyli.cpp
@@ -65,15 +65,19 @@
 		if (raja > 0)
 		{
 			Vector kohta = lerpVector(kasvustot[i][raja-1], kasvustot[i][raja], frac);
-			glVertex3fv((float *)&(kasvustot[i][raja-1]+kasvustomovedeltas[i]*aika));
-			glVertex3fv((float *)&(kohta+kasvustomovedeltas[i]*aika));
+			Vector delta = kasvustomovedeltas[i]*aika;
+			Vector vtmp0 = kasvustot[i][raja-1]+delta;
+			Vector vtmp1 = kohta+delta;
+			glVertex3fv((float *)&vtmp0);
+			glVertex3fv((float *)&vtmp1);
 		}
 		glEnd();
 		glBegin(GL_LINE_STRIP);
 		for (j=0;j<raja;j++)
 		{
 			glColor4f(0.7f, 0, 0, alpha);
-			glVertex3fv((float *)&(kasvustot[i][j]+ kasvustomovedeltas[i]*aika));
+			Vector vtmp = kasvustot[i][j]+kasvustomovedeltas[i]*aika;
+			glVertex3fv((float *)&vtmp);
 		}
 		glEnd();
 	}
--- a/src/Effects/solut.cpp
+++ b/src/Effects/solut.cpp
@@ -119,7 +119,8 @@
 			{
 				const float r = 0.005f;
 				const float kohta = k / 7.0f;
-				glVertex3fv((float *)&(solujutut[i][j] + Vector((float)sin(kohta*2*3.141592f), (float)cos(kohta*2*3.141592f), 0)*r));
+				Vector vtmp = solujutut[i][j] + Vector((float)sin(kohta*2*3.141592f), (float)cos(kohta*2*3.141592f), 0)*r;
+					glVertex3fv((float *)&vtmp);
 			}
 			glEnd();
 		}
