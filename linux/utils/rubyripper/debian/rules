#!/usr/bin/make -f

#export GEM2DEB_TEST_RUNNER = --check-dependencies
CONFIGURE_EXTRA_FLAGS += --enable-gtk3 --enable-cli --enable-lang-all

%:
	dh $@ --with ruby

override_dh_auto_configure:
	dh_auto_configure -- $(CONFIGURE_EXTRA_FLAGS)
override_dh_auto_build:
