#!/usr/bin/make -f

export APP_PATH=/usr/bin/
export PKG_CONFIG_PATH=/usr/lib/$(DEB_HOST_MULTIARCH)/pkgconfig/

%:
	dh $@

override_dh_auto_install:
	find . -name "vera_mono.ttf" -delete
	find . -name "vera_sans.ttf" -delete
	dh_auto_install
