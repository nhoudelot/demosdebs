#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@

override_dh_strip:

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/303-by-acme.6.md -o debian/303-by-acme.6
