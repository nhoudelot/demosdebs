#!/usr/bin/make -f

%:
	dh $@

override_dh_shlibdeps:
	dh_shlibdeps -XlibGLEW.so.1.5 -- --ignore-missing-info

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/simreich-by-contraz.6.md -o debian/simreich-by-contraz.6
