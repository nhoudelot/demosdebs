#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = hardening=+all
#flto dont work in this package

%:
	dh $@

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/drfungi-by-fit.6.md -o debian/drfungi-by-fit.6
