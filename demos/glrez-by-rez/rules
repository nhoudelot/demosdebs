#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/glrez-by-rez.6.md -o debian/glrez-by-rez.6

override_dh_shlibdeps:
	dh_shlibdeps -- -l.
