#!/usr/bin/make -f

%:
	dh $@

override_dh_shlibdeps:
	dh_shlibdeps -- -l.

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/uranusflower-by-thebadsectors.6.md -o debian/uranusflower-by-thebadsectors.6
