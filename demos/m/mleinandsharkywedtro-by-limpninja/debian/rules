#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/mleinandsharkywedtro-by-limpninja.6.md -o debian/mleinandsharkywedtro-by-limpninja.6
