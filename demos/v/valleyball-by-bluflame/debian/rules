#!/usr/bin/make -f

%:
	dh $@

override_dh_shlibdeps:
	dh_shlibdeps -- -l. --ignore-missing-info

override_dh_install:
	dh_install
	pandoc -s -t man -f markdown debian/valleyball-by-bluflame.6.md -o debian/valleyball-by-bluflame.6
